from django.urls import path

urlpatterns = [
    # path('', CartItemCreateAPIView.as_view(), name='create-cart'),
    # path('update/<int:pk>/', CartItemUpdateAPIView.as_view(), name='update_cart'),
    # path('checkout', CartCheckoutView.as_view(), name='checkout_cart'),
]
